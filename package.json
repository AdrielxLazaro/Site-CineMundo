{
  "name": "site-cinemundo",
  "version": "0.1.0",
  "private": true,

  "dependencies": {
    "json-server": "^1.0.0-beta.5"
  },

  "scripts": {
    "start": "react-scripts start",
    "server": "json-server --watch db.json --port 3001"
  }
}